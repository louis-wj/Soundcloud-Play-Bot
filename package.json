{
  "name": "soundcloud-playbot-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "compile-backend": "cd backend && \"C:\\Users\\User\\AppData\\Local\\Programs\\Python\\Python310\\python.exe\" build_backend.py",
    "dist": "npm run compile-backend && npm run dist --prefix frontend",
    "build-all": "npm install && npm run compile-backend && npm run dist"
  }
}